[package]
name = "clonedir"
version = "0.1.0"
authors = ["Jeff Dickey <@jdxcode>"]
license = "MIT"
description = "clone a directory with copy-on-write"
repository = "https://github.com/jdxcode/clonedir_lib"

[dependencies]
clonedir_lib = "0.1.0"
